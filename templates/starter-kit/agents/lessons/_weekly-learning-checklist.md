---
week: W??
---

# Weekly Learning Checklist — W??

<!--
HOW THIS FILE WORKS (the DC parser ignores this comment block):

  - Each H2 header `## agent-name` starts a bucket for that agent.
  - Bullet points under that H2 become individual topics for that agent.
  - The H2 sections `## Приоритет недели` and `## Общие (любой агент)`
    are reserved — the parser skips them. Use them for human notes that
    should not be routed to any specific agent.
  - Sub-headers (`### ...`) reset the bucket — anything under them is
    treated as free text, not a topic.
  - The `week:` value in frontmatter is just a label shown on the Topics
    page.

Edit this file weekly. The Dreaming Center reads it on every visit to
/p/{slug}/topics and injects the topics into the /self-study prompt for
each agent during the nightly cron.

EXAMPLE (drop your real agent names where the placeholder is):

  ## aba-architect
  - review accounts→users migration
  - check spec for new payment flow

  ## frontend-architect
  - consistency of Modal in new forms
-->

## Приоритет недели

- (one sentence describing this week's overall focus)

## Общие (любой агент)

- (topics every agent should be aware of — major refactors in flight, etc.)
